#include "Blockchain.h"
#include "hash.h"
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {

	vector<int> listOfNodes;

	char input;
	cout << "Are you the initial Node? (Y or N) ";
	cin >> input;

	if (input == 'Y' || input == 'y') {
		// Code to begin a new block-chain
	}
	else {
		// Code to join an existing block-chain
	}



	return 0;
}